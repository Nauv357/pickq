# 更新日志（Changelog）

本项目桌面端的更新日志。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号倒序排列。

> **以 GitHub Releases 与更新频道为准。**
> 应用内「检查更新」拉取的公告来自更新频道 `https://pickq.cn/updates/latest.json`，
> GitHub Releases 为备用渠道；本文件是这两处的**汇总记录**，可能滞后于实际发布。
>
> **维护提示（每次发版时）**
> - 新增条目时，措辞遵守 [`docs/release-notes-guide.md`](docs/release-notes-guide.md)：
>   **面向用户可感知的功能变化，只写「新增 / 优化 / 修复」，不写安全细节、运维/基础设施事项与内部实现**
>   （不写「仅监听 127.0.0.1」「加强限流」「升级依赖修复漏洞」这类描述）；
> - 版本号以 `tauri/src-tauri/tauri.conf.json` 为准，`tauri/src-tauri/Cargo.toml` 必须与之一致；
> - 发版流程见 `docs/release-notes-guide.md` 的检查清单（构建 → 上传更新频道 → 官网下载链接 → 线上验证 → GitHub Release）。

## [Unreleased]

### 新增
- 题库详情页只剩一个主按钮**「开始练习」**：点一下直接开练，题目已经按「错题 → 今天到期复习 → 你题量最少的知识点 → 还没做过的题」配好；练习页顶部有一句**可核对**的说明，例如「这 20 题：8 题是你之前做错的 · 6 题今天到期复习 · 6 题来自「资料分析·增长」（这个知识点你库里只有 8 题）（题库共 125 题）」——看完点 ✕ 收起即可。想改题量或自己定范围，点「开始练习」右边的小箭头。
- **错题讲解**（成绩报告 → 展开任意错题 →「讲给我听」）：一次讲清三段，边写边显示：
  - **错在哪**：针对你实际选的选项说清为什么不对，以及你为什么会这么选（概念混了 / 公式用错 / 漏看条件）；
  - **这类题怎么做**：这类题的识别特征 + 可以照着做的固定步骤，不只是讲这一道；
  - **下次防错**：一句话的检查动作，下次做题时具体看什么、算什么。
  - 可以先点一个错因（**看错 / 蒙的 · 这个知识点不会 · 完全没见过**）——不是必填，选了会按你的真实原因讲；讲完可以在下面继续追问。
- **讲解现在是唯一的解析入口**（成绩报告 / 练习历史 / 题库列表行内都用同一个）：题做错过就讲「错在哪」，没做过或做对了就讲「这道题怎么做」，风格不再有两套；**离开再回来会把你上次听过的讲解原样显示出来**（标着"上次讲于 …"），不用再花一次额度，也能接着追问。讲完有两个去处：**「存为解析」**写进题库（会随题库文件分享），或**「存进笔记」**留在自己这儿。
- **我的笔记**（侧边栏「笔记」；做题页与练习历史里每题也有「记一笔」）：把做题时的想法、记忆钩子、AI 讲得好的一段记下来。笔记**只存在本机、不会随题库文件分享给别人**（和"解析"是两件事：解析属于题库，笔记属于你）。一条笔记**可以挂到多个题库、多道题**上（比如同一类题的好几道），也可以谁都不挂——挂在哪里随时能加能解，删题库/删题**只会解除关联，不会删掉你写的内容**（它会留在笔记页的「未归类」里）。
- **用另一个题库文件给已有题库加题**（题库详情 → 题目区「**从题库包追加…**」）：选一个 `.zip` / `.tiku` 题库文件（也可以直接粘贴 JSON），题就接到当前题库后面（题号接着排），里面重复的题会自动跳过；**当前题库的名称、版本、导出范围都不受影响**。以前只能"导入成一个新题库再合并"。
- **AI 私教**：做题时不会的题可以点「提示」按级取用（**第 1 级只指方向 → 第 2 级给关键一步 → 第 3 级才给完整解析**，不会一上来就把答案给你），已给过的级别会标出来；成绩页还有**复盘**——先用公式统计这一场的错题（按知识点分布、该知识点的历史正确率），再让 AI 只依据本场错题讲一遍。所有回答都是**边写边显示**，并且会存档，重新打开这道题能接着上次聊。
- 题库可以**自动标注知识点**了（题库详情页 →「更多」→ 知识点）：应用会把题目对应到一张「技能图」上的知识点，用来做**筛选、讲解与统计**的口径（不会拿它给你判定"你掌握了没有"）。AI 结果先作为**建议**列出，确认后才生效；自己也能改，人工改过的题不会再被 AI 覆盖。标签只保存在本机，导出题库时会随包带走（作者确认过的标签）。
- **词表可以自己改了**（知识点页 →「管理知识点」）：内置的两张技能图不再只能照单全收——
  - **自定义知识点**：新增、**改名**、换到别的阶段、删除（改名不会让已经打好的标签失效）；
  - **官方知识点**：用不上的可以**停用**（只是在你这里隐藏，不动官方模板），随时恢复；
  - 一行「**恢复官方模板**」把这张图上的本机改动全部撤销。
- **按难度挑题（85% 规则，不用管它在后台做什么）**：每次作答都会更新这道题的难度估计，之后配题会优先挑**预测成功率 80%–90%** 的题——太简单不涨水平，太难只是打击信心。同一个知识点练熟之后，练习会自动混入相邻知识点的题（交错练习），没练熟之前则集中刷同一个点。

### 优化
- **「笔记」页重新排过**：按时间分成**今天 / 昨天 / 最近 7 天 / 最近 30 天 / 更早**（小标题跟着滚动吸顶），
  每条显示"13:12 / 昨天 14:05 / 3 天前 / 9月4日"；长笔记默认折叠成 6 行，想看全文点「展开全文」；
  一条笔记可以标一个**类别色**（黄/绿/蓝/粉，卡片左侧出现色条），列表里一眼分得清；
  一行文字不再拉满整个屏幕（阅读宽度收窄），条目之间有了明确的层级。
- **笔记里关联的题目可以就地看了**：点笔记里的「第 N 题」直接展开题干、共用材料、选项（正确答案高亮）、
  答案与解析，看完收起或点「去题库」进完整页面——以前只有一个跳转链接，看一眼就得离开笔记页。
- **笔记不再只能是纯文字**：选中文字就能**高亮**（黄/绿/蓝/粉四色）、**加粗**、**划线**，
  想清掉就点「清除标记」；写的时候有"效果预览"。笔记本来就不是文档，这几样对应纸面上的荧光笔与圈画。
- **正文大小可以自己调了**（设置 →「外观与语言 → 正文大小」，笔记页右上角也有 A−/A+）：
  小 / 标准 / 大 / 特大四档，默认比以前大一点；题目、选项、材料、解析、讲解、笔记正文一起变，
  标题与按钮不受影响。
- **笔记可以按内容搜**：笔记页顶部一个搜索框，直接找"我记过什么"。
- **练习成绩页与「练习历史」里的逐题回顾现在是同一套**（以前是两份实现，同一场练习在两个入口看到的东西不一样）：每题直接给出**题干、共用材料、选项（对错高亮）、我的作答、正确答案与解析**，主观题当场就能给分，下面照样能「讲给我听」和「记一笔」。
- 笔记的编辑框会**跟着内容长高**（写长了不用在小框里来回滚），显示与编辑用同一套规则（公式、图片都能正常显示）；只有内容里确实有公式或图片时才在下面给一块预览。
- 「我的笔记」里的「第 N 题」现在真的能跳回去了（此前点了没反应）：会翻到那道题所在的页、展开它的讲解面板并滚到视野中间。（接上一版，命名也一并统一为侧边栏「笔记」。）
- 三处"加题"入口的名字按门槛排开：**AI 追加 / 从题库包追加… / 新增题目**；共用材料的说明改成一句普通话（不再举例），错题讲解下面那句"承诺式"的提示删掉了。
- 做题页的快捷键提示改成**与实现一致**（原先写死「A~H」，实际按题目的选项字母判断，选项超过 8 个时提示是错的），并支持英文界面。
- 题库详情页的头部从 8 个按钮收成 **1 个主按钮 + 「更多」**：打开题库就是"开始练"——「练习历史、我的笔记、知识点、打印试卷、导出题库文件、编辑题库、删除题库」收进「更多」，其余低频操作不再和主路径抢位置。
- 题目区的操作不再一排平铺：只留三种"加题"方式（**AI 追加 / 从题库包追加… / 新增题目**，名字说清各自怎么加），「共用材料 / AI 补答案 / 选题另存」收进题目区的「更多」。
- 同一屏不再有两个主按钮：「今日待复习 N 题」改为次级样式（到期题本来就包含在「开始练习」的一键配题里）。
- 「我的笔记」在「更多」菜单里显示条数，和「错题（N）」「收藏（N）」一个口径。
- 「开始练习」改成**可拆开的按钮**：点左边直接开练（默认 20 题），点右边的小箭头可以**选题量**（10 / 20 / 50 / 100）或走「自定义练习…」自己定范围和题量——题量与范围不再藏在「更多」里。
- 题目的分类字段**只保留一个**：原来的「主题」和「分类」两个框合并为「试卷 / 章节」（这题从哪来：哪套卷、哪一章），「分类」不再出现在任何界面。你的题目数量、筛选、打印、导出都不受影响；老题库里只填过「分类」的题，会在题目列表上方提示一句，点一下就能把它并入「试卷 / 章节」（原有内容不会被覆盖）。编辑器里的「册数」也一并收起来了（一直没有实际用途）。
- **AI 导入会按原文的卷名/章名自动填「试卷 / 章节」**（如「2024 年国考行测」「第二章 函数」），导入后题库本身就有组织；原文没有这种层级标题就留空。
- 「知识点」界面重做，改成**逐题审阅**：每题一行，显示题号、题型、题干，以及当前标签（可就地修改）和它的来源（你标注的 / AI 建议，带把握分）。
  - **标签能直接在这里改**：不用再跳到题目编辑页——行内下拉选完即生效；「详情」按钮仍然可以打开题目看完整内容，但已经不是改标签的必经之路。
  - **下拉里可以直接输入新知识点**：图里没有的名称会立刻建成「自定义知识点」并打在这题上（同名会复用，不会出现两个看起来一样的）。这样 AI 判错、或你的题库里有词表里没有的知识点时都有地方补。
  - 顶部的「已确认 / 待确认 / 未匹配 / 全部」是可点击的筛选（数字就是题数），不再是三个页签来回切；还能按知识点筛选，下拉里直接标出每个知识点你有多少题、哪些知识点你一道题都没有。
  - 行内「看题干」就地展开题干、选项、答案与解析，审阅时不用来回跳页。
  - **多选更方便**：可以「全选本页」，也可以一键「选中全部 N 题」（含未显示的页，几百题也只需一次请求），再批量「设为知识点 / 确认建议 / 丢弃建议」。
  - 覆盖情况是三个**互不重叠**的口径：已确认 / 待确认 / 未匹配（相加 = 总题数），另单列「可用于判定掌握」的高把握题数；界面上的每个数字都等于按该状态列出的题目条数。此前两个口径混在一起展示，数字看起来自相矛盾（如「待标注 26」与「待确认 16」）。

### 修复
- **做完练习后展开一道题，现在能看到题干了**：此前只给答案与解析，题干要往上滚回列表里找，等于"展开"没意义。现在每题把题干、材料、选项（对错高亮）与答案/解析一起给出，展开的题和"练习历史"里看到的完全一致。
- **同一道题在不同页面给出的"对/错"结论现在一致了**：审计发现「最近一次作答算不算错」这条规则在代码里被写成了 5 份，其中统计页与错题本的口径**恰好相反**（判题正确但自评"部分对"的记录，一个算对、一个算错）；另外"没配答案 / 主观题没自评"的题在成绩报告里是"不判对错"，但在会话详情与练习历史里被算成了**答错**。现在这条规则只有一处实现，五个出口共用，并加了回归用例把它钉住。
- 做题页的**键盘快捷键恢复可用**：方向键切题、字母键选答案、数字直达题号此前全部失灵。原因是页面上有一个（看不见的）弹层残留，而快捷键的守卫写成了"只要存在弹层就不响应键盘"——现在只在弹层**真的开着**时才不抢键盘（也因此不会误伤"在追问输入框里打字"）。
- 删除题目、删除材料时的确认按钮此前写着「**删除题库**」（复用了同一个词条）；「恢复复习计划」的取消按钮写的是「再想想」——现在按钮文案都等于动作本身、取消统一叫「取消」。
- 统计页的「错题治愈 / 最近练习」在**加载失败时不再伪装成"还没有记录"**（失败会说明原因并给出重试）；「我的笔记」页保存失败也会说话了。
- 把题目编辑器里的「试卷 / 章节」**删空**现在会真的生效（此前清空后保存，旧值会"自己回来"）。
- 题目的知识点标签在**题目详情**与「知识点」页面显示不一致（详情里会多出一条 `gk.zl.concept` 这样的内部编号）：原因是详情把"技能图升级后已不存在的旧标签"也显示了出来、而且直接显示了内部编号。现在两处**只认当前技能图**：失效标签不再显示、不再参与统计，并在知识点页面提示「有 N 条旧标签已失效」，点一下即可清理干净。
- 题目编辑器里的「知识点」此前拿不到技能图（下拉是空的、已有标签也不显示），现在打开编辑就能看到并直接修改；从知识点界面点「详情」进来时这一栏会自动滚到视野中间并高亮。

## [0.1.20] - 2026-09-14

### 优化
- 导出的题库文件后缀从 `.tiku` 改为 `.zip`：容器本来就是标准 zip，现在下载后可以直接双击解压查看图片与题目，不必先手动改后缀。**旧文件不受影响**——`.tiku` 仍然可以正常导入，广场上已发布的 `.tiku` 作品下载文件名也保持原样。

## [0.1.19] - 2026-09-14

### 新增
- AI 导入新增支持 **Excel（.xls / .xlsx）、CSV、PowerPoint（.ppt / .pptx）与老版 Word（.doc）**：表格按行列、幻灯片按页抽出文字后交给 AI 整理成题目。
- **PPT 与 Excel 里的图片也会一起提取**（不需要开启 MinerU）：幻灯片按页取图，Excel 按图片所在的那一行把图配到对应题上，导入预览的素材区即可看到，AI 也会据此把图放到题目里。同一张图（如页眉图标）不会在每页重复出现；被整页只有一张图的纯图片题也会保留。
- 题库列表可以直接管理题库：每张卡片右上角的「…」或**右键**即可打开题库操作（开始做题、练习历史、打印试卷、重命名、导出、删除），不必再先进题库详情页。
- 「批量管理」：勾选多个题库后可一次性导出、合并为新题库或删除（支持按住 Shift 连选）。
- 题库详情里勾选若干题目后，底部操作条新增「导出所选」（可直接导出成题库文件）与「删除所选」；导出时新增范围选择：全部 / 仅收藏 / 仅错题 / 仅未做过 / 已勾选。
- 题库详情里的题目编辑改为独立大窗口：编辑时列表不再被顶到页面顶部，右侧题号盘可以继续用来跳题。

### 优化
- 设置里 MinerU 的说明改为一句加粗的结论：「除非你导入后发现大量题目缺图，否则不需要配置」，打开高级设置一眼就能看到；导入预览的素材区底部也加了同样口径的指引（缺图时去哪里配置、配置后重新导入）。
- MinerU（扫描件版面解析）不再常驻导入界面：平时不显示，导入照片/扫描件/纯图时才会主动提示；需要时可在「更多选项」里开启。
  此前它只对 PDF 和图片显示，导入 Word / Excel / PPT 时根本找不到这个开关。设置页里对应的 Key 也收进「高级（可选）」，不配置不影响任何常用功能。
- 题库卡片信息重新排版：标题、题数、版本/作者/时间各占一行，长标题自动省略；卡片直接显示题数与已做进度，不必点进去看。
- 题目列表每行的操作按钮从 5 个精简为「收藏 + 编辑 + …」，其余操作（从这题开始做题、AI 解析、复制题干等）收进「…」或右键菜单。
- 右键改为应用自己的菜单（此前弹出的是系统浏览器菜单，其中的「另存为 / 共享」没有实际作用）；输入框内仍保留系统菜单，方便右键粘贴。
- 题库描述、来源、版本号等字段可容纳更长的内容，与题库广场允许的长度保持一致（此前从广场下载的题库文件若描述较长，导入时会失败）。
- AI 导入失败时会说明更具体的原因（模型超时、请求过于频繁、无法连接、返回内容无法解析、文档解析失败等），便于自行判断是重试、换模型还是检查文件。

### 修复
- **「导入题库文件（.tiku）」点了没反应的问题**：选完文件后请求其实没有发出去，刷新页面才发现结果（含图题库或较慢的机器上更明显）。现在会正常导入，并在导入过程中显示「正在导入题库…（已用 N 秒）」，让等待过程可见。
- 下载与导入的等待过程不再"黑箱"：从题库广场下载并导入时应用内会显示进度与已用时间，下载超时会说明"网络较慢或文件较大，可先用浏览器下载该文件，再在题库页点「导入」选择本地文件"；官网的文件下载现在会显示真实进度，中断后可以从断点继续，下载按钮下方也给出"卡住了怎么办"（复制直链 / 重新下载）。
- 开启 MinerU 解析 PowerPoint / Excel 时，文档里的配图不再丢失：此前这类文档里的插图会被整批丢弃，预览页的素材区是空的（PDF 不受影响）。现在 PPT 的插图会按页提取，Excel 表格里的图片会提取到它所在的那一行题目上。
- 重复导入同一批题目（同一个 AI 导入任务确认两次、或把同一份题库文件再次导入同一个题库）时不再报错，已存在的题目会自动跳过，不会产生重复题目。
- 删除题库时更彻底地清理该题库的数据，避免残留数据在之后导入同一份题库时引发偶发错误。
- 练习历史页的标题此前显示为内部英文标识，现已正常显示中文。
- 修复导出「部分题目/某个分类」时可能被记成整个题库发布身份的问题（会导致之后判断"内容是否改过"出错）。

## [0.1.17] - 2026-09-11

### 修复
- 题目内容没有实际改动时，离开编辑区不再弹出「未保存」提示。

### 优化
- 编辑区的「展开全部 / 折叠全部」合并为一个按钮，点击即在两种状态间切换。

## [0.1.16] - 2026-09-11

### 优化
- AI 导入预览改为「先直接看内容、需要修改时再进入编辑」，不再是打开就进编辑态。
- 支持一键展开 / 折叠全部题目。

## [0.1.15] - 2026-09-11

### 新增
- AI 模型可在应用内按自己的 API Key 动态获取真实可用列表，不再只依赖内置的几个模型名。
- 模型预设支持从远端更新；当所选模型已下线时，应用会给出可替代的模型建议。
- 支持免 API Key 的本地模型（Ollama），零成本使用 AI 整理。

## [0.1.14] - 2026-09-10

### 新增
- 发布中心：查看导出记录、「导出到文件夹」、直接从磁盘上的 `.tiku` / `.json` 文件发布。
- 选择导出目录时使用系统文件夹选择对话框。

### 优化
- 「我的作品」在离线时也可以查看。

## [0.1.13] - 2026-09-10

### 修复
- 更新下载过程中点击「取消 / 停止」，弹窗立即关闭，不再等待。

## [0.1.12] - 2026-09-10

### 新增
- 发布前先在本地校验题库文件（`.tiku` / `.json`）并预览元数据，文件不合规时当场提示，不会白跑一趟网络请求。

### 优化
- 举报受理后的作品下架处理流程更完整。

## [0.1.11] - 2026-09-10

### 新增
- 可在应用内把题库发布到题库广场，并在「我的作品」里管理自己发布过的内容。
- 桌面端支持用 GitHub 账号登录广场。
- 密码输入框可切换显示明文，便于确认输入。

## [0.1.10] - 2026-09-10

### 新增
- 可以用**用户名或已验证的邮箱**登录。
- 注册增加人机验证，减少批量注册。

### 优化
- 桌面端若干界面文案调整。

## [0.1.9] - 2026-09-10

### 新增
- 题库广场账号可在应用内登录 / 注册；支持收藏、评论、点赞（第一阶段）。
- 应用内可直接浏览广场作品并一键导入。

### 优化
- 界面文案与国际化的收尾完善。

## [0.1.8] - 2026-09-10

### 新增
- 自动更新支持直接下载并安装；下载可停止、可继续。

## [0.1.7] - 2026-09-09

### 新增
- 界面支持**中英双语**：默认跟随系统语言，可在「设置」中切换。

## [0.1.6] 及更早

维护者待补充：仓库的 git 历史从 `0.1.7`（首个带版本号提交）开始记录，
`0.1.0`–`0.1.6` 的变更明细没有可追溯的提交记录。
已知事实：`README.md` 提到「已安装旧版本的用户，打开应用即会收到更新提示（v0.1.1 起内置自动更新）」。

---

## 版本号与链接

- 桌面端当前版本：`0.1.20`（`tauri/src-tauri/tauri.conf.json`、`tauri/src-tauri/Cargo.toml`）
- 更新频道：<https://pickq.cn/updates/latest.json>
- 下载页（官网）：<https://pickq.cn>
- GitHub Releases（备用渠道）：资产名形如 `PickQ_<版本>_x64-setup.exe` / `PickQ_<版本>_portable.zip`，
  tag 为 `v<版本>`。**链接维护者待补充**——发版规范里记录的仓库路径是 `Nauv357/pickq`
  （据此推导为 `https://github.com/Nauv357/pickq/releases`，待维护者确认后可直接写进本文件）。
- 后端 Maven 版本号（`pom.xml`）固定为 `0.0.1-SNAPSHOT`，与桌面端版本号**不同步**，属预期。
