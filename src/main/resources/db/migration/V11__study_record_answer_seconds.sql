-- ============================================================
-- V11: 会话交卷统一判分（2026-09 交互模型调整）
-- 做题中不再逐题提交；交卷时一次性落库，服务端无法再从"相邻提交时间差"
-- 推导每题用时 → 每题用时由前端本地累计后随交卷提交（study_record.seconds），
-- 旧数据（NULL）仍按作答时间差兜底推算。
-- ============================================================
ALTER TABLE study_record ADD COLUMN seconds BIGINT;
